spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vendor_db
    username: root
    password: newpassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: false